import 'package:flutter/material.dart';

class PkatPage extends StatelessWidget {
  const PkatPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('PKAT'),
        centerTitle: true,
        backgroundColor: Colors.blue[400],
      ),
    );
  }
}